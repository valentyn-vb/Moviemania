# TMDB API key (v3 auth). Get one at https://www.themoviedb.org/settings/api
# Server-only — never prefix with NEXT_PUBLIC_ (that would inline it into the client bundle).
TMDB_API_KEY=your_tmdb_api_key_here
