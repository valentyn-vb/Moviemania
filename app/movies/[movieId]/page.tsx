// The base /movies/[movieId] view. All shared movie content lives in the
// layout; Cast and Reviews render into the layout's {children} via their tabs.
export default function MovieOverviewPage() {
  return null;
}
